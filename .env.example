# Local dev only — matches docker-compose.yml. `make dev` copies this to .env
# if absent. No real secrets belong here (SPEC review #5: targets.auth is
# encrypted/reference-only; this DSN is a throwaway local credential).
PLUMBER_DATABASE_URL=postgres://plumber:plumber@localhost:5433/plumber?sslmode=disable
